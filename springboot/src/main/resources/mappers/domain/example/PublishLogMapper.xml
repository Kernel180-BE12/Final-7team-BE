<insert id="insertPublishLog" parameterType="com.example.PublishLogEntity">
    INSERT INTO publish_log (ai_content_id, channel_id, status, platform_post_id, platform_url,
    response_body, error_message, idempotency_key, attempt_count)
    VALUES (#{aiContentId}, #{channelId}, #{status}, #{platformPostId}, #{platformUrl},
    #{responseBody}, #{errorMessage}, #{idempotencyKey}, #{attemptCount})
</insert>