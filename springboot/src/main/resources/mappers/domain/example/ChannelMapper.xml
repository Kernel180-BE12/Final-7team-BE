<select id="selectByIds" parameterType="java.util.List" resultType="com.example.Channel">
    SELECT channel_id as channelId, name, platform, api_url as apiUrl, content_format as contentFormat,
    secret_ref as secretRef, rate_limit_per_min as rateLimitPerMin, enabled,
    meta, created_at as createdAt
    FROM channel
    WHERE channel_id IN
    <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
        #{item}
    </foreach>
</select>